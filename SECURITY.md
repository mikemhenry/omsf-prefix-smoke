# Security policy

Please do not open a public issue for a suspected vulnerability or publishing-credential problem.

Report security issues through the private security-reporting mechanism on the GitHub repository. Include the affected package filename, version, build string, channel, SHA-256 digest, and any relevant workflow run URL.

The maintainers may yank a package while investigating. Published artifacts should not be overwritten because doing so invalidates existing lock files and provenance records.
